@model CMS.Data.Entities.Post

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Thêm bài viết</title>

    <style>
        body {
            font-family: Arial;
            background: #f5f5f5;
            padding: 30px;
        }

        .container {
            width: 650px;
            margin: auto;
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 0 10px #ccc;
        }

        h1 {
            text-align: center;
            margin-bottom: 25px;
        }

        label {
            font-weight: bold;
        }

        input, select {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        textarea {
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-top: 5px;
            margin-bottom: 15px;
            padding: 10px;
            min-height: 180px;
        }

        .btn-save {
            background: #28a745;
            color: white;
            border: none;
            padding: 12px;
            width: 100%;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

            .btn-save:hover {
                background: #218838;
            }

        .btn-back {
            display: inline-block;
            margin-top: 15px;
            text-decoration: none;
            color: #007bff;
        }
    </style>

    <!-- CKEditor 5 (CDN) -->
    <script src="https://cdn.ckeditor.com/ckeditor5/39.0.1/classic/ckeditor.js"></script>
</head>

<body>
    <div class="container">
        <h1>THÊM BÀI VIẾT</h1>

        <form asp-action="Create" asp-controller="Post" method="post" enctype="multipart/form-data" asp-antiforgery="true">
            <div>
                <label asp-for="Title">Tiêu đề</label>
                <input asp-for="Title" />
                <span asp-validation-for="Title" style="color:red"></span>
            </div>

            <div>
                <label asp-for="Content">Nội dung</label>
                <textarea asp-for="Content" id="Content"></textarea>
                <span asp-validation-for="Content" style="color:red"></span>
            </div>

            <div>
                <label>Hình ảnh</label>
                <input type="file" name="ImageFile" accept="image/*" />
            </div>

            <div>
                <label asp-for="CategoryId">Danh mục</label>
                <select asp-for="CategoryId" asp-items="ViewBag.Categories">
                    <option value="">-- Chọn danh mục --</option>
                </select>
                <span asp-validation-for="CategoryId" style="color:red"></span>
            </div>

            <button class="btn-save" type="submit">Thêm bài viết</button>

            <a class="btn-back" href="/Post">← Quay lại danh sách</a>
        </form>
    </div>

    <script>
        ClassicEditor
            .create(document.querySelector('#Content'), {
                // cấu hình mặc định
            })
            .catch(error => {
                console.error(error);
            });
    </script>
</body>
</html>
